What happens when you initialize a repository? Why do you need to do it?

A ".git" file is created that contains metadata about the current directory.

How is the staging area different from the working directory and the repository? What value do you think it offers?

The staging area allows you to group related files into a single logical commit. From the SA, you commit to a repository. The working diretory is where you have are you files including the ".git file"